public class mathematical_tasks {
    public static void main(String[] args) {

     // The Math.max(x,y) method can be used to find the highest value of x and y:

     System.out.println("The highest number is:"+Math.max(2,5));
     System.out.println("The lowest number is:"+Math.min(2,5));
     System.out.println("The square root of number is:"+Math.sqrt(64));

    //  The Math.abs(x) method returns the absolute (positive) value of x:
    System.out.println(Math.abs(-89.76));

    //  Math.random() returns a random number between 0.0 (inclusive),
    //  and 1.0 (exclusive)

    System.out.println(Math.random());

    // To display a random number between 0 to 100, you can use the following formula:

    System.out.println("\n");
    int  random_num = (int)(Math.random()*101);

    System.out.println(random_num);

   

    int Random = (int)(Math.random()*101);
    System.out.println(Random);
    
    int Random1 = (int)(Math.random()*201);
    System.out.println(Random1);













        












    }
    
}
